{
  "name": "dist-transactions",
  "version": "1.0.0",
  "description": "Distributed transactions example",
  "type": "module",
  "scripts": {
    "init-db": "bun run src/scripts/init-db.ts",
    "start:foo": "NODE_ENV=development bun run --watch src/foo-service/server.ts",
    "start:bar": "NODE_ENV=development bun run --watch src/bar-service/server.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.26.0",
    "pino": "^8.x.x",
    "postgres": "^3.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "bun-types": "latest"
  }
}
